from pandas import DataFrame

from models.daybook import Daybook


def convert(activities_books: list[DataFrame], cash_books: list[DataFrame]):
    # TODO Merge the dataframes into one data sorted dataframe.
    # TODO Convert the new dataframe into a list of accounting records
    return Daybook()
