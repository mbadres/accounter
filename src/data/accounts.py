from models.account import Account
from models.account_type import AccountType

accounts = {
    3100: Account(
        3100,
        AccountType.INCOME,
        "Allgemeine Spenden",
        6000,
    )
}
